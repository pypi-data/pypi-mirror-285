[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "BackpackTFv2"
version = "0.0.3"
authors = [
  { name="BrBriz"},
]
description = "Backpack TF API Library"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
]