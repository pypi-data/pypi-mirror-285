[project]
name = "yt-dlp-youtube-accesstoken"
readme = "README.md"
requires-python = ">=3.11"
version = "0.1.1"

[project.license]
text = "Apache-2.0"

[build-system]
requires = [
    "setuptools",
]
build-backend = "setuptools.build_meta"

[tool.distutils.bdist_wheel]
universal = true
