[build-system]
requires = ["setuptools>=64", "setuptools_scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "negeng"
dynamic = ["version"]

authors = [
  {name = "Collin Meyer", email = "collinmmeyer@gmail.com"},
]
maintainers = [
  {name = "Collin Meyer", email = "collinmmeyer@gmail.com"}
]

description = "Negative Engineering library for designing in the negative space"
readme = "README.md"

requires-python = ">= 3.10"

dependencies = []

license = {file = "LICENSE"}

# [project.optional-dependencies]
# gui = ["PyQt5"]
# cli = [
#   "rich",
#   "click",
# ]

# [project.scripts]
# spam-cli = "spam:main_cli"

# [tool.setuptools_scm]

