[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "prismbreak"
version = "1.1.0"

[dependencies]
numpy = ">=1.21.0"
matplotlib = ">=3.4.0"
Pillow = ">=8.0.0"