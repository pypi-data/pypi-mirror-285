[project]
name = "gittool"
version = "0.1.4"
description = "gittool"
authors = [
    { name = "Jimmy Lin", email = "b00502013@gmail.com" },
]
dependencies = []
requires-python = ">=3.12"
readme = "README.md"

[project.license]
text = "MIT"

[project.scripts]
use = "asd.use:cli"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool.pdm]
distribution = true
