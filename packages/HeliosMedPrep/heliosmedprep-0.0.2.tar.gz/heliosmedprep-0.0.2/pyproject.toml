[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "HeliosMedPrep"
authors = [
    {name = "Duc Le", email = "ngocducdarkzonezero@example.com"}
]
description = """HeliosMedPrep"""
version = "0.0.2"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "requests > 2.26.0",
    "pandas",
    "numpy",
    "torch",
    "tqdm",
    "scikit-learn"
]