[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "xbinary_classifier"
version = "0.4.0"

dependencies = ["numpy","seaborn","scikit-learn","matplotlib","joblib","pandas","astropy","warnings"]

