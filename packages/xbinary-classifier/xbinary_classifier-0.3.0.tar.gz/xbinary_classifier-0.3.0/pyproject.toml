[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "xbinary_classifier"
version = "0.3.0"

dependencies = ["numpy","seaborn","scikit-learn","matplotlib","joblib","pickle","pandas","astropy","warnings"]

