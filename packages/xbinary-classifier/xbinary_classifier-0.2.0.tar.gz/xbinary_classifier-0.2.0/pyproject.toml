[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "xbinary_classifier"
version = "0.2.0"

dependencies = ["numpy","seaborn","scikit-learn","matplotlib"]

