[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "leiaa"
version = "0.0.0"
authors = [
  { name="Augmetec Ltd. (A Company Registered in England & Wales)" },
]
description = "The leading investigations platform"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]