[tool.poetry]
name = "flare-utilities"
version = "0.1.1"
description = "Flare Utilities having support libraries for Flare-BDD framework for reporting purposes."
authors = ["Aravinth Alliraj"]
readme = "README.md"
packages = [{ include = "flare_utilities" }]

[tool.poetry.dependencies]
python = "^3.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
