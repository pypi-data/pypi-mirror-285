[build-system]
requires = ["maturin", "poetry"]
build-backend = "maturin"

[project]
name = 'stringanalysis'
version = "0.5.3"

[project.optional-dependencies]
test = [
    "pytest",
    "mypy"
]

deploy = [
    "setuptools",
    "maturin"
]