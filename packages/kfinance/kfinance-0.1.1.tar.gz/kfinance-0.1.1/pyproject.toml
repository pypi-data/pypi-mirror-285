[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kfinance"
version = "0.1.1"
description = "Python package for Korea finance"
readme = "README.md"
license = { text = "MIT" }
authors = [
    { name = "Jungwoo Yang", email = "jwyang0213@gmail.com" }
]
dependencies = []
