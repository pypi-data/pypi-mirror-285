{
    "version": "0.2.0",
    "configurations": [        
        {
            "name": "mtmai",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "cwd": "${workspaceFolder}/src/mtmai",
            "args": ["main:app", "--reload", "--host", "0.0.0.0", "--port", "8201"],
            "jinja": true
        }
    ]
}