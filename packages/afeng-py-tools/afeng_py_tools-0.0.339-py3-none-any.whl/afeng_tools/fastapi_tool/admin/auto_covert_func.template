from fastapi import Form
from typing import Optional
from datetime import datetime
from decimal import Decimal
{{enum_import_str}}

async def convert_form_to_{{module_code|safe}}_model_func({{params_str|safe}}):
    return {{args_str|safe}}
