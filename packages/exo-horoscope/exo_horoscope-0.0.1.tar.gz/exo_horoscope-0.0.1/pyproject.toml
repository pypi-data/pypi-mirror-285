[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "exo_horoscope"
version = "0.0.1"

dependencies = [
    "numpy",
    "datetime",
    "geopy",
    "astropy",
    "astroquery"
]