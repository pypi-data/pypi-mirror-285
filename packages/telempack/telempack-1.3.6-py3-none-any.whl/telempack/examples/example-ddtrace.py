"""
Example 3: Demonstration of using ddtrace

In order to use ddtrace, you must configure a datadog agent. The list of requirements are below
    - Datadog organization api key
    - Datadog agent configured (there are multiple ways to do this)
    - Enabling the usage of datadog profiler (optional and the default = false)
    - Datadog endpoint link (reccomended to store as an environment variable)
"""
