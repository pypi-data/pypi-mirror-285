[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "dlspark"
version = "1.0.2"
dependencies = ["numpy", "networkx", "matplotlib"]
requires-python = ">= 3.12"
authors = [{ name = "Eric Lin", email = "erix025@outlook.com" }]
