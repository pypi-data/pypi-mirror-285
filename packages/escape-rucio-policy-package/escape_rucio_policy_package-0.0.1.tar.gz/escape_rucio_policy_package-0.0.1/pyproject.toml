[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "escape-rucio-policy-package"
version = "0.0.1"
authors = [
    { name="ESCAPE CERN", email="escape-cern@cern.ch" },
]
description = "Rucio policy package for the ESCAPE collaboration"
readme="README.md"
requires-python = ">=3.9"
classifiers = [
   "Programming Language :: Python :: 3",
   "Operating System :: OS Independent",
]

[tool.setuptools]
py-modules = []
