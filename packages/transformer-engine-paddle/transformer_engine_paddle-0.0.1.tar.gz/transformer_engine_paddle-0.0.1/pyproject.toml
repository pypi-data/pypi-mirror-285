[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "transformer_engine_paddle"
version = "0.0.1"
description = "A placeholder package, the full package will be uploaded in the near future."

