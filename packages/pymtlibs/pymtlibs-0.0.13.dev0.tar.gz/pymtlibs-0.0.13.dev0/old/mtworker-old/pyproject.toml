[tool.poetry]
name = "mtworker"
version = "0.1.11"
description = ""
authors = ["a <a@a.com>"]
readme = "README.md"
packages = [{include = "mtworker"}]
[tool.poetry.dependencies]
python = "^3.10"
typer = {extras = ["all"], version = "^0.7.0"}
requests = "^2.28.2"
playwright = "^1.29.1"
httpx = "^0.23.3"
celery = {extras = ["redis"], version = "^5.2.7"}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[project.scripts]
mtworker = "mtworker.__main__:cli"